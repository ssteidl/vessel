#!/usr/bin/env tclsh8.6
# -*- mode: tcl; indent-tabs-mode: nil; tab-width: 4; -*-

set jail_params {

}

FROM FreeBSD:12.0-RELEASE

CWD /

COPY . /app

#EXPOSE 80

#Run in a jail when building
RUN env ASSUME_ALWAYS_YES=yes pkg install bash uwsgi curl
RUN bash
